import React from "react";
import {Text, View} from "@/components/Themed";

const QrCodeScanner = () => {

    return (
            <Text style={{display: "flex", fontSize: 20, justifyContent: 'center', alignItems: 'center', width: '100%', height: '100%', backgroundColor: 'black'}}>
                QrCodeScanner isn't work for web!
            </Text>
    )
}

export default QrCodeScanner;